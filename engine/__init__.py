"""
Vocal Chords — the "Bridge" layer.

High-level interface to ygo-env / OCGCore: env setup, action mappings
(Selection/Activation), and state cloning so the Brain can drive
simulations without touching Lua directly.
"""
